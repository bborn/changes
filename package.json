{
  "name": "jazz-practice",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "import:tunes": "node scripts/import-tunes.mjs",
    "deploy:public": "node scripts/deploy.mjs public",
    "deploy:personal": "node scripts/deploy.mjs personal",
    "deploy:all": "npm run deploy:public && npm run deploy:personal"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "4.129.0"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "vexflow": "^4.2.5"
  }
}
